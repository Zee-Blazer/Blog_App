import { colors } from './colors';
import { containerSpacing, lineHeight, space } from './spacing';
import { fontSizes, fontWeights } from './fonts';

export const theme = {
    colors,
    space, 
    containerSpacing,
    lineHeight, 
    fontSizes, 
    fontWeights
};